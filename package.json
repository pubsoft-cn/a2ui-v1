{
  "name": "a2ui-shell",
  "version": "1.0.0",
  "description": "A2UI Shell - Cross-platform SDUI rendering engine with dual-channel fetching",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint 'src/**/*.ts'",
    "screenshots": "npx tsx scripts/capture-screenshots.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "playwright": "^1.59.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
